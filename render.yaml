services:
  - type: static
    name: resumeai
    repo: https://github.com/prasad-kadagalla/ResumeAI
    branch: main
    buildCommand: npm ci && npm run build
    publishPath: dist
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
